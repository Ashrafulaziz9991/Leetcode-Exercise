#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    vector<int>ratings= {};

    int mx_cnt = 0, mx = ratings[0];

    for (int i = 0; i < ratings.size(); i++)
    {
        
    }
    

    return 0;
}